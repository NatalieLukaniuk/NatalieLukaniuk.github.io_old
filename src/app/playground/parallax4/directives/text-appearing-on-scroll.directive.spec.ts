import { TextAppearingOnScrollDirective } from './text-appearing-on-scroll.directive';

describe('TextAppearingOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAppearingOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
