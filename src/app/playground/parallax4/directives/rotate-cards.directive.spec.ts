import { RotateCardsDirective } from './rotate-cards.directive';

describe('RotateCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateCardsDirective();
    expect(directive).toBeTruthy();
  });
});
