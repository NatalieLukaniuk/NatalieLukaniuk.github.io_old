import { TextTransformationsOnScrollDirective } from './text-transformations-on-scroll.directive';

describe('TextTransformationsOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new TextTransformationsOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
